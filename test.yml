  - name: Repository Dispatch
        uses: peter-evans/repository-dispatch@v1
        with:
          token: ${{ secrets.ACTION_TOKEN }}
          repository: MrBayene/sp-E2ECheckoutFlow
          event-type: external-call
          client-payload: 'TEST'
